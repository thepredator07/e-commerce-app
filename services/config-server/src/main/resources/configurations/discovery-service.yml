eureka:
  instance:
    hostname: localhost
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/

server:
  port: 8761


#eureka:
#  instance:
#    hostname: localhost
#  client:
#    register-with-eureka: false
#    fetch-registry: false
#    serviceUrl:
#      # Corrected the URL format for defaultZone
#      defaultZone: http://${eureka.instance.hostname}:8761/eureka/
#
#server:
#  # Specified the correct port for the Eureka server
#  port: 8761